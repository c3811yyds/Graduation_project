feat: 完善文档与增加邮箱注册/登录体系及防乱码处理

详细更新内容：
1. 重构 `README.md`：
   - 修复并清理了所有早期的排版冗余和重复乱码内容。
   - 补充了所有的后端 API 路由清单以及 `notes` 随堂笔记关联表结构说明。
   - 校正了 Windows 下虚拟环境激活的常用命令，更改了箭头排版样式。
2. 数据库与数据定义层 (`database_seed.sql` & `backend/models.py`)：
   - 为用户表补充 `status` 字段及其默认值，解决 500 插入异常。
   - 核心新增了唯一键 `email`，支持邮箱功能的数据存取；优化了时间戳设计 (`updated_at`)。
   - 修复了因为 PowerShell 导出 SQL 时发生字符重定向导致的中文“绗竴璁”乱码问题，现已使用 utf8mb4 标准输出。
3. 后端业务层 (`backend/routes_auth.py`)：
   - 修改注册接口 `register`，加入基本的 `@` 邮箱格式校验、邮箱唯一性检测机制。
   - 修改登录接口 `login`，通过 `|` 条件将“用户名”或“邮箱”合并统一为跨身份验证入口。
4. 前台界面 (`frontend/src/components/AuthModal.vue`)：
   - 对于注册表单，新增邮箱输入框 (`type="email"`)。
   - 对于登录表单，Placeholder 和 Label 智能提示切换为 `用户名 / 邮箱`。
   - 修补并在关闭/打开弹窗时添加了所有响应式变量的清理与复位机制。
